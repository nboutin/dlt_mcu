/**
 * \file dlt_stdlib.h
 * \brief DLT standard library
 * \date 2024-04
 * \author Nicolas Boutin
 */
#pragma once

#define DLT_min(a, b) (((a) < (b)) ? (a) : (b))

#define DLT_max(a, b) (((a) > (b)) ? (a) : (b))
